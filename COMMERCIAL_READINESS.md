# 상용 출시 잔여 과제 (COMMERCIAL READINESS)

작성 2026-09-01. **이 문서는 자동 개발의 최우선 백로그다.** 위에서부터 소진한다.

## 원칙
- `[ ]` 미완, `[x]` 완료. 완료 시 근거(파일·테스트)를 한 줄로 남긴다.
- **build now, activate on approval**: 코드는 끝까지 만들되 실인증·실결제·실개인정보·실발신 **활성화는 사람 승인**. 스위치는 환경변수로 분리하고 기본 OFF.
- 임의 성과·KPI 수치를 화면·문서에 넣지 않는다. 실측 전에는 기능 서술로 쓴다.
- 모든 변경은 테스트·빌드 검증 통과 후 커밋한다.

## 공통 상용 필수 (전 제품)
- [x] **에러 모니터링** — `api/monitoring.py`(의존성 0, Sentry envelope). chat·assist·ops_stats 핸들러 except 배선, `/api/health.monitoring` 상태 노출. `tests/test_monitoring.py` 16건 통과(no-op·PII 마스킹·DSN 미하드코딩·전송실패 격리). DSN은 Vercel 환경변수 `SENTRY_DSN` **[승인 필요: 사람이 등록]**
- [x] **구조화 로깅** — `api/_log.py`(의존성 0, JSON 1줄/요청). request_id(인바운드 x-request-id·x-vercel-id 승계)·duration_ms·error_code, 응답 헤더 `X-Request-Id`. PII 미기록: 쿼리스트링 제거·예외 메시지 미기록·필드 마스킹, 기본 접근로그(쿼리 PII 유출) 침묵. chat·assist·ops_stats 배선. `tests/test_logging.py` 21건 + E2E 스모크 통과
- [x] **/health 확장** — `api/health.py`에 `version`(build·commit·branch·env·region)과 `dependencies[]`(llm·order_backend·speech·cpaas·monitoring·storage: status/required/detail/latency_ms) 추가. required 기준 `status`=healthy|degraded|unhealthy 산출, 하위호환 키 유지. 기본 shallow(네트워크 무접속)·deep 도달성 점검은 `HEALTH_DEEP=1`+`?deep=1` 동시 충족시 자격증명 없이 TCP만. `tests/test_health.py` 26건 통과(비밀값 미노출·부작용 없음·예외 격리·상태 산출) + E2E GET/HEAD 200
- [x] **표준 에러 응답 + 입력검증** — `api/_errors.py`(의존성 0). 전 라우트 공통 봉투 `{ok:false, error(문자열·하위호환), code, status, request_id, details?, event_id?}`. 내부 예외 문구(`str(e)`) 응답 제거 — 진단은 `CALLBOT_DEBUG_ERRORS=1` 일 때만 scrub 경유. 예외 분류(400/413/502/504/500), 4xx 는 모니터링 미전송(알림 노이즈 차단). 입력검증: 본문 상한(기본 1MiB·STT 8MiB)·타입·길이·화이트리스트(chat.messages, assist.task, ops_stats.period, stt.mime, tts.text, voice 본문). `_guard.deny` 도 같은 봉투. `tests/test_errors.py` 39건 + 전체 103건 통과, 로컬 E2E 400/403/200 확인
- [x] **rate limit** 공개 API 적용 — `api/_ratelimit.py`(의존성 0). 경로 등급별 한도: llm(chat·assist) 20/분·speech(stt·tts) 12/분·webhook(voice) 120/분·default 40/분. IP 한도만으로는 X-Forwarded-For 위조로 우회되므로 **등급별 전역 한도**(과금 상한, 기본 240/120/1200/600)를 함께 둔다. 전역 차단시 IP 카운터를 소모하지 않아 정상 사용자가 이중 벌점을 받지 않는다. 429는 표준 봉투 + `Retry-After`·`X-RateLimit-*`(`_errors.send(extra_headers=)`), 키 사전 상한 5000으로 메모리 방어, 제한 로직 장애는 허용 쪽으로 폴백(가용성 우선). `/health.ratelimit` + dependencies 노출(IP 원문 미포함). 한계: 서버리스 인스턴스 로컬 카운터라 클러스터 정확 쿼터가 아님 — 정밀 쿼터 필요시 공유 저장소 배선 **[승인 필요]**. `tests/test_ratelimit.py` 31건 + 전체 134건 통과, 로컬 E2E 200×3→429(헤더 포함) 확인
- [x] **접근·감사 로그** — `api/_audit.py`(의존성 0). 관리 기능(`/api/ops_stats`·`/api/voice`·`/api/health?deep`) 접근만 별도 스트림(`kind=audit`)으로 분리 기록: 액션·결과(allow/deny/error)·상태코드·actor(자격 종류 + 지문)·client(IP 솔트 해시)·ua 대분류·request_id. **거부도 기록**(403/401/429) — 침해 조사에서 실패 시도가 더 중요. PII·비밀값 미기록: 원문 IP·API 키·웹훅 토큰·쿼리스트링·본문 제외(키는 sha256 앞 8자 지문만, 틀린 키 반복 시도는 지문으로 묶임). append-only(수정·삭제 API 없음), 버퍼 유실은 `evicted` 카운터로 드러냄, 감사 실패는 요청을 죽이지 않음(가용성 우선). `/health.audit` + dependencies 에 **개수 요약만** 노출. 한계: 인스턴스 메모리 버퍼는 휘발 — 영속 보관은 플랫폼 로그 수집 의존, 변조방지(WORM) 저장소·HTTP 조회 화면·고정 솔트(`CALLBOT_AUDIT_SALT`)는 **[승인 필요]**. `tests/test_audit.py` 29건 + 전체 163건 통과, 로컬 E2E 200(allow)/403(deny) 2건 기록·IP 원문 미유출 확인
- [x] **백업·복구 절차** — `RUNBOOK.md`. 백업 대상 인벤토리(무상태 구조상 '데이터 복구'가 아니라 '서비스 재구성'임을 명시)·RTO/RPO 목표·정기 오프라인 번들 절차·복구 시나리오 5종(배포 롤백/저장소 손상/Vercel 유실/시크릿 유출/의존 서비스 장애). 리허설은 문서가 아니라 실행으로 검증: `scripts/restore_drill.py`(의존성 0, 읽기 전용·네트워크 미사용) 가 백업→번들 무결성→빈 폴더 복원→HEAD 일치→핵심파일→py_compile→HTML 파싱 7단계를 실제 수행. 2026-09-03 리허설 **7/7 성공·26.4초**(번들 0.9MiB, api 20개, public 7개) 기록. 영속 스토리지 스냅샷·번들 자동화·Vercel 설정 백업은 **[승인 필요]**
- [ ] **약관·개인정보 처리방침 확정본 반영** (현재 초안, 문안은 사람이 확정)
- [ ] **테스트** 핵심 로직 커버리지 확보, CI에서 실행 — *CI 완료 · 커버리지 진행 중*: `.github/workflows/ci.yml`(push·PR·수동, 시크릿 불요, `permissions: contents:read`)에서 pytest + 커버리지 보고 + `scripts/verify.py` 릴리스 게이트 + `scripts/restore_drill.py` 복구 리허설 실행. 게이트는 로컬·CI가 **같은 스크립트**를 쓴다(문법·HTML 파싱·중복 id·금지어 §13-1·복지 잔재 §13-5). 2026-09-03 2차: `tests/test_escalation.py` 47건 추가 → 210건, 커버리지 37% → 47%(`engine` 0→88%, `escalation` 0→74%, `sim_call` 0→60%, `order_backend` 0→55%). 2026-09-05 3차: `tests/test_voice.py` **65건** 추가 → 전체 **275건 통과**, 커버리지 **47% → 59%**(`voice` 0→**93%**). 웹훅 인증(토큰 헤더·`?t=`·거부)·입력검증(413/400)·이벤트 파싱(clawops·sim)·상태전이(answered/speech/completed)·중복 이벤트·VoiceML 분기·XML 이스케이프·과금/PII 안전을 덮는다. 회귀로 결함 4건 발견·수정(아래 'CPaaS 웹훅' 항목). **남은 0%**: `ops_stats`(133)·`speech_providers`(99)·`sip_adapter`(86)·`recording_audit`(85)·`tts`(77)·`chat`(48), `assist`·`stt` 29%·`_guard` 66%. 2026-09-07 4차: `tests/test_guard_stt.py` **41건** 추가 → 전체 **316건 통과**, 커버리지 **59% → 64%**(`_guard` 66→**81%**, `stt` 29→**89%**). 오리진 판정(동일출처·허용목록·리퍼러 폴백·curl 거부)·자격 경로(API 키·STRICT 잠금·웹훅 토큰 헤더/`?t=`)·거부 봉투 규약(429 `Retry-After`, 설정 힌트·API 키 미노출)·가용성 폴백(_ratelimit 부재/예외 시 허용)·STT 입력검증(mime 화이트리스트·413·400)·엔드포인트 GET/POST(키 원문 미노출, sim 프로바이더)를 덮는다. 모든 경로에서 `urlopen` 을 감시해 **업스트림(Gemini) 미호출**을 강제 — 테스트가 과금되지 않는다. **남은 0%**: `sip_adapter`(86)·`tts`(77)·`chat`(48), `recording_audit` 31%·`assist` 29%. 2026-09-09 5차: `tests/test_ops_stats.py` **51건** 추가 → 전체 **465건 통과**, 커버리지 **64% → 72%**(`ops_stats` 0→**69%**, 셀프테스트 블록 제외 시 실효 94%). 집계 산식(기간별 합계·자동처리율·연결감축·baseline 주입)·스키마 고정(소스가 죽어도 키·타입 불변)·게이트 플래그 읽기전용(조회가 실발신 스위치를 켜지 않는다)·부작용 없음(큐 불변·`urlopen` 감시로 업스트림 미호출)·HTTP 계약(200/400/403, 표준 봉투, `no-store`, 요청ID 승계, CORS 되비침 금지)·감사(allow/deny/error)·비밀값·식별자 미노출을 덮는다. 회귀로 결함 3건 발견·수정: (1) **감사 기록이 결과보다 먼저** 남아 500 으로 끝난 요청도 `allow/200` 으로 적히고 400 은 아예 기록되지 않았다 → 결과 확정 후 실제 상태로 기록 (2) **감사 호출 실패가 요청을 죽였다**(try 안) → 감사 예외를 격리(가용성 우선) (3) `period` 가 문자열이 아니면(list·dict) `in` 이 TypeError → today 폴백. 덤으로 응답에 `data_source="demo"` 를 실어 **수치가 실측이 아님을 소비자가 알 수 있게** 했다(새 수치 추가 아님 · §8 준수). 2026-09-10 6차: `tests/test_chat_assist.py` **76건** 추가 → 전체 **541건 통과**, 커버리지 **72% → 76%**(`chat` 0→**99%**, `assist` 29→**98%**). 접근 가드(403·프리플라이트·CORS 되비침 금지)·입력검증(messages·phone·scenario·task·text, 400/413 봉투와 `details[].field`)·대화 이력 항목 검증·시나리오 배선·assist JSON 파싱(코드펜스·본문혼합·비JSON·배열)·PII 미노출(대화 원문·발신번호·API 키)·업스트림 장애 분류(502/504)를 덮는다. `urlopen` 감시로 **업스트림 미호출**을 강제(테스트 과금 0). 회귀로 결함 3건 발견·수정: (1) **`scenario` 가 버려졌다** — 콘솔·react 가 보내는 시나리오를 `chat` 이 `run_turn` 에 넘기지 않아 안부·연체·청렴도 대본이 **항상 기본(주문/환불) 프롬프트**로 응답했다 → 화이트리스트 검증 후 전달, `GET /api/chat.scenarios` 로 목록 노출, 콘솔 버튼 키와의 드리프트를 회귀로 감시 (2) **잘못된 대화 이력이 500** — `content` 없는 user 항목이 engine 안에서 KeyError 로 터져 사용자 입력 오류가 내부 오류로 보고(모니터링 알림 노이즈)됐다 → 항목별 400 + `messages[i].field` 지목, engine 은 2차 방어 (3) **모델의 비정형 출력이 500** — `assist` 가 본문 속 `{...}` 를 재파싱하다 실패하면 `{"raw":...}` 폴백을 뚫고 예외가 올라갔다 → `parse_json()` 으로 분리해 코드펜스·배열·깨진 중괄호까지 계약(dict) 유지. 2026-09-14 7차: `tests/test_tts_recording.py` **57건** 추가 → 전체 **598건 통과**, 커버리지 **76% → 79%**(`tts` 0→**90%**, `recording_audit` 31→**73%**, 셀프테스트 블록 제외 시 실효 100%). TTS: 접근 가드(403·CORS 되비침 금지·speech 등급 429 `Retry-After`)·입력검증(text 필수 `details[].field`·1000자 경계·공백 strip)·프로바이더 분기(기본/edge 합성 경로, sim 메타 JSON 오디오 0, clova/google/aws 는 SPEECH_LIVE OFF 에서 sim 강제)·health(합성 없이 200, 리포트 실패도 200·내부 경로 미노출, pending 프로바이더 `[승인 필요]` 표기)·장애 분류(ImportError 500/URLError 502/timeout 504, 예외 문구 속 성명·전화번호 미반영). 녹취: RECORDING_LIVE 게이트 파싱·미동의/게이트 OFF 시 원문 참조 폐기(이중 방어)·등록이 게이트를 켜지 않음·접근 목적 화이트리스트(정확 일치, 거부·미스 감사, 불허 목적은 존재 여부도 미노출)·보관 만료 경계값·hard_delete/anonymize·재파기 무기록·문서화 스키마와 레코드 키 일치. `urlopen` 감시 + `_synth` 대역으로 **edge_tts·네트워크 미호출** 강제(테스트 과금 0). 회귀로 결함 2건 발견·수정: (1) **감사 로그 반환값이 얕은 복사**라 항목 dict 를 고치면 원본 감사기록이 변조됐다(escalation 에서 고친 것과 같은 결함) → 항목까지 복사 (2) **미지 파기 방식이 조용히 hard_delete 로 처리** — `anonymise` 오타면 정책과 다른 파기가 감사기록에 남는다 → `PURGE_METHODS` 검증 후 ValueError, 파기 전 거부. **남은 0%**: `sip_adapter`(86). 2026-09-14 8차: `tests/test_sip_adapter.py` **42건** 추가 → 전체 **640건 통과**, 커버리지 **79% → 82%**(`sip_adapter` 0→**89%**, 셀프테스트 블록 제외 시 실효 100%). 이벤트 모델(순서·필수 키·미지 타입/방향 거부)·sim 인바운드(발화 주입·빈 발화·타입 거부·종료 후 정리·중복 hangup)·dry-run 발신(billed 0·반환 사본·입력검증·보관 상한 200)·실발신 게이트(CPAAS_LIVE=1 이어도 sim 은 PermissionError, 실회선 골격 3종 항상 거부, `"1"` 정확 일치만 ON)·팩토리(게이트 OFF 면 무조건 sim, 미지명은 sim 폴백)·health(비밀값·원문 번호 미노출, 골격은 ok=False `[승인 필요]`)를 덮는다. `urlopen`·`socket.create_connection` 감시로 **네트워크 미사용** 강제. 회귀로 결함 3건 발견·수정: (1) **리스너 예외가 통화를 중간에 끊어** `_active` 에 통화가 영원히 남았다(누수) → 리스너별 격리, 예외는 `listener_errors` 로 health 에 드러냄(삼키지 않음), 리스너에는 사본 전달(변조 격리) (2) **이벤트·dry-run 기록에 원문 전화번호**가 그대로 실렸다 → `mask_phone`(voice._mask_phone 과 동일 규칙, 회귀로 일치 강제) (3) `dial()` 이 빈 문자열·비문자열·자릿수 미달을 **조용히 기록** → ValueError 로 거부, 잘못된 시도는 기록되지 않음. 덤으로 게이트를 import 시점 상수가 아니라 호출 시점(`is_live()`)에 읽어 배포 후 환경변수 변경이 반영되고, `_event` 의 assert(−O 에서 사라짐)를 ValueError 로 교체. **남은 0% 없음**. 2026-09-16 9차: `tests/test_order_backend_sim.py` **57건** 추가 → 전체 **697건 통과**, 커버리지 **82% → 84%**(`order_backend` 55→**100%**, `sim_call` 60→**97%**). 인터페이스 계약(기반 클래스 NotImplementedError·dispatch 비공개/미지 툴 거부·engine.TOOLS 전 항목 구현 드리프트)·Demo(한도=합계·견적 이름/수량/미지 상품 0원·주문 주입)·Http(URL·Bearer 조건부·본문 UTF-8·빈 본문·backend_unavailable·쓰기 미승인 dry-run 시 urlopen 미호출)·팩토리(기본 demo·BASE 없으면 폴백·캐시·`"1"` 정확 일치)·sim 대본(목록 고정·chat 시나리오/콘솔 버튼 드리프트·대본 내 전화번호/주민번호 없음)·simulate(순서·이력 누적·전환 조기 종료·phone/scenario 전달)·HTTP 계약(403/200/400/500/502/429/401·봉투·요청ID·no-store·CORS 되비침 금지)을 덮는다. `urlopen` 감시로 **네트워크 미사용** 강제. 회귀로 결함 3건 발견·수정: (1) **`sim_call` 이 모르는 시나리오를 조용히 refund 대본으로 바꿔** 실행했다 — 요청한 시나리오가 검증된 것처럼 보이고 LLM 토큰도 엉뚱한 대본에 썼다 → 400 `details[].field=scenario`, LLM 미호출 (2) **engine 장애가 200 속 `error`** 로, `run_turn` 예외는 본문 없는 500 으로 새어 나갔다 → 표준 봉투(500/502)·구조화 로그·요청ID (3) **`DemoOrderBackend.lookup_recent_order` 가 얕은 복사**라 호출자가 `items` 를 고치면 인스턴스 수명 동안 데모 주문이 변조됐다 → 깊은 복사. 덤으로 `/api/sim_call` 요율 등급을 default(40/분) → **llm(20/분)** 으로: sim 1회가 LLM 3~8회라 chat 의 16배까지 열려 있었다. 다음 순서 제안: `voice` 91%·`_errors` 88% 잔여 분기

## Callbot(AICC Portal) 전용 (준비도 ~52%)
- [x] **통화 개인정보 암호화·파기 배선** — `api/pii_vault.py`(의존성 0) + `api/recording_audit.py` 배선. 녹취 참조(`audio_ref`·`transcript_ref`)는 RECORDING_LIVE+동의가 모두 참일 때만 받고 **받는 즉시 봉인**해 보관한다. **평문 폴백 없음**: 키(`PII_MASTER_KEY`)가 없으면 평문으로 저장하는 대신 참조를 폐기하고 그 사실을 감사기록(`register_refs_dropped`)에 남긴다(조용히 넘어가지 않는다). 구성은 HKDF-SHA256 유도 → HMAC-SHA256 카운터 키스트림 → encrypt-then-MAC(표준 라이브러리만, 새 암호 원시함수 없음). AES-GCM 교체점은 `_encrypt`/`_decrypt` 두 함수로 모아뒀다. **문맥 결속**: 봉투는 `REC-0007/audio` 같은 문맥에 묶여 다른 레코드·다른 필드로 옮겨 심으면 열리지 않는다. **키 회전**: `PII_MASTER_KEY_OLD`(복호 전용) + `rewrap()`. **파기는 둘로 나눈다** — 만료 파기(`purge_due`)는 봉투를 통째로 버리고, 만료 전 즉시 파기(`shred_record`, 정보주체 삭제요구 등)는 복호 재료만 지운 **묘비**를 남겨 백업에 사본이 있어도 복구 불가이면서 파기 사실은 증명된다. 복호는 `reveal()` 한 경로뿐이고 목적 화이트리스트·감사기록을 거치며, 실패(키 분실·변조)를 빈 값으로 위장하지 않고 예외로 드러낸다. `tests/test_pii_vault.py` **51건** + 기존 녹취 회귀 갱신 → 전체 **895건 통과**. 회귀로 결함 1건 발견·수정: **패딩 없는 base64 의 가단성** — 마지막 글자만 바꾼 다른 문자열이 같은 바이트로 풀려 봉투 하나가 여러 표기를 갖고 변조 탐지도 헛돌았다 → 정규 표기만 허용(되감아 비교). 키 등록은 Vercel 환경변수 **[승인 필요: 사람이 등록]** — 미설정이 기본이고, 그 상태에서는 아무 원문도 저장되지 않는다.
- [x] **발신번호 등록 상태 관리 화면** — `api/caller_id.py`(의존성 0) + 콘솔 「관리 → 발신번호 등록」(`#callerid`). 전기통신사업법 발신번호 사전등록 대장: 번호별 등록 상태·증빙·만료를 한 곳에서 관리한다. **증빙 없이 승인 없다** — `verified`·`renew` 는 증빙 종류(화이트리스트)와 발급일이 있어야 하고, 발급 후 90일을 넘긴 서류는 거부한다. **만료는 저장하지 않고 계산한다** — 저장된 상태가 `verified` 여도 만료일이 지나면 조회 결과는 `expired` 다(대장이 실제보다 좋아 보이지 않는다). 만료 30일 전부터 임박 경고. **번호 원문을 남기지 않는다** — 화면·목록·이력은 마스킹 번호만 쓰고(voice._mask_phone 과 같은 규칙, 회귀가 일치를 강제), 원문이 필요하면 `pii_vault` 로 봉인하되 키가 없으면 아예 보관하지 않는다. 사용 중지 시 번호 원문은 암호 파기. **조회가 스위치를 켜지 않는다** — `CPAAS_LIVE` 를 읽기만 하고, `outbound_ready` 는 등록 요건 충족일 뿐 실발신 허용이 아니다(**[승인 필요]** 표기). 품질: 빈 상태·로딩(`role=status`)·오류(`role=alert`)·필드별 인라인 검증(`details[].field` 반영)·`aria-live`·라벨·Enter 키 실행, 되돌리기 어려운 동작(중지·반려)에 확인 절차. 증빙 종류는 하드코딩하지 않고 정책 API 에서 받는다(드리프트 차단). `tests/test_caller_id.py` **55건** + `tests/test_console_caller_id.py` **27건**. 회귀로 결함 2건 발견·수정: (1) **만료된 등록을 사용 중지할 수 없었다** — 전이표가 `expired` 를 받지 않아 대장을 닫지 못하고 번호 원문이 남았다 → `revoke`·`renew` 의 허용 상태에 `expired` 추가 (2) **10자리 `010` 번호가 통과했다** — `01[016789]\d{7,8}` 이 2004년 통합 이후 존재하지 않는 형식을 받아 등록 후 통신사에서 반려될 번호였다 → `010` 은 11자리 고정으로 분리. 저장은 인스턴스 메모리(영속 저장소 **[승인 필요]**).
- [x] **AI 고지 문구 테넌트별 설정 화면** (법정 요구) — `api/disclosure.py`(의존성 0) + 콘솔 「관리 → AI 고지 문구」(`#disclosure`). 통화 첫 발화를 테넌트별로 설정하되 **요건을 규칙으로 검사**해 미충족 문구는 저장되지 않는다: AI 응대 명시(영문은 단어 경계 — 'AICC' 의 'AI' 는 불인정)·운영 주체(`{brand}` 자리표시자, 재생 시 치환)·상담사 연결 안내(권고)·**녹음 고지와 `RECORDING_LIVE` 일치**(녹음 안 하면서 '녹음됩니다'도, 녹음하면서 침묵도 허위 고지)·길이 10~300자·전화번호/주민번호·타사명(`scripts/verify.py` BANNED 와 일치 강제)·마크업. 기본 문구는 게이트 ON/OFF 모두에서 요건 충족. `voice.py` 배선: 우선순위 테넌트 설정 > `CALLBOT_GREETING`(하위호환, 요건 검사 결과는 `env_override` 로 드러냄) > 기본 — 어떤 경우에도 고지가 빈 문자열이 되지 않는다. 종전 기본 인사말 「콜봇 상담센터입니다」는 AI 명시가 없어 기본 문구를 교체했다. 이벤트 `tenant_id`(clawops `metadata.tenant_id`) 로 테넌트 식별, 비문자열은 무시. API: `GET`(기본·요건·한도·env_override) / `?tenant=` 실효 문구+검사 / `?op=list|history` / `POST {tenant_id,text,brand,dry_run}` / `POST {tenant_id,op:reset}`. 버전 증가·append-only 이력(최근 100, 주체=감사 actor 지문)·테넌트 상한 200·반환값 사본. 접근 가드 + **감사 스트림 `tenant.disclosure`**(허용·거부·오류 모두). 콘솔: 요건 검사(dry-run)·저장(확인창)·기본 복귀(확인창)·실효 문구 불러오기·테넌트 목록·이력, 인라인 검증(`details[].field` 반영)·글자수·빈/오류/로딩 상태·라벨·`aria-live`·Enter. 화면은 서버 검사 결과만 그린다. 테스트 `tests/test_disclosure.py` **50건** + `tests/test_console_disclosure.py` **14건** → 전체 **761건 통과**, 커버리지 **85%**(`disclosure` 96%). 로컬 E2E(실제 소켓): info 200 → 미충족 400(ai_identity) → dry-run 200 → 저장 200(v1) → 테넌트 조회 → 이력 → reset → 잘못된 id 400 → 외부 오리진 403. **한계·[승인 필요]**: 저장은 인스턴스 메모리(휘발) — 영속 저장소·관리자 인증 확정 후 배선. 최종 법정 문안은 사람이 확정한다.
- [x] 상담사 에스컬레이션 실동작 검증 (sim 시나리오 회귀 테스트) — `tests/test_escalation.py` 47건(정책 4트리거 우선순위·경계값, 큐 상태전이 계약·append-only 감사, 엔진 배선 3경로=툴전환·가드전환·홉소진, sim `handoff` 스크립트 회귀, 발신번호 미기록·네트워크 미사용). 회귀로 결함 2건 발견·수정: (1) `engine.run_turn` 이 `escalate_to_agent` 성공 시 `transferred` 를 갱신하지 않아 전환이 **한 턴 늦게** 보고되던 문제 → 해당 턴에서 즉시 확정 (2) `EscalationQueue.audit_log()` 의 얕은 복사로 감사 기록 원문이 외부에서 변조 가능하던 문제 → 항목 복사. 실상담원 배정·CTI 연동은 **[승인 필요]**
- [x] **CPaaS 웹훅 회귀 검증** — `tests/test_voice.py` 65건(인증·입력검증·파싱·상태전이·중복이벤트·VoiceML·안전), 네트워크·LLM·STT 미호출(대역 + urlopen 감시). 회귀로 결함 4건 발견·수정: (1) **XML 속성 주입** — `CallId`·발신번호가 텍스트 이스케이프만 거쳐 `" onX="` 로 VoiceML 속성을 덧붙일 수 있었다 → `_xattr`(따옴표 이스케이프) + action URL 퍼센트 인코딩 (2) **녹음 콜백 중복 처리** — 같은 `RecordingUrl` 이 재배달되면 STT·LLM 이 다시 돌고 환불 같은 도구가 **두 번 실행**됐다 → 세션에 직전 응답을 기억해 그대로 재생 (3) **`answered` 재배달이 대화 이력을 초기화** → 세션이 있으면 유지 (4) `metadata` 가 dict 가 아니면 500 → 무시하고 기본 시나리오. 추가로 통화 로그(`/api/voice?op=log`, 콘솔이 4초마다 폴링)의 **발신번호 마스킹**(`010****2222`) — 화면은 번호를 쓰지 않아 기능 손실 없음. 실회선 연동·서명검증(CPaaS 제공 서명)은 **[승인 필요]**
- [x] **운영 대시보드 지표 실데이터 연결 준비** — `api/call_metrics.py`(의존성 0) + `api/voice.py` 배선 + `api/ops_stats.py` 의 **별도 `measured` 블록** + 콘솔 「모니터 → 실측 통화 지표」 카드. 데모 수치를 실측처럼 보이게 만드는 대신, **이 서버가 실제로 처리한 통화만** 세어 데모와 분리해 내보낸다(헤드라인은 여전히 `data_source="demo"`, 실측 블록은 `"measured"`). **없는 수치를 만들지 않는다**: 표본 0건이면 비율·평균은 `null` 이고 화면은 `—` 로 그린다 — 0%로 내리지도, 데모 값으로 채우지도 않는다(0%도 주장이다). **부분 표본을 기간 값으로 위장하지 않는다**: 관측 창이 요청 기간보다 짧으면 `partial=true`+`window_sec` 로 드러낸다(2시간치로 '월간'을 만들지 않는다). 판정은 추정이 아니라 관측 규칙이고(`transferred`=엔진이 전환을 결정, `bot_completed`=전환 없이 봇 응대 턴 1회 이상, `abandoned`=응대 턴 0, `failed`=busy/no-answer/failed), 소비자가 '봇 자동완결'을 과대해석하지 않도록 응답에 `definition` 을 함께 싣는다. **원문을 담지 않는다**: 통화ID 는 HMAC 지문(앞 12자)으로만 쓰고 외부에 내보내지 않으며, 시나리오·테넌트 라벨에 번호형 값이 오면 저장하지 않는다. **오류를 삼키지 않는다**: 수집 실패가 통화를 끊지 않도록 예외는 전파하지 않되 `collector.errors`·`orphan_finishes`·`dropped` 로 드러내고 화면이 경고한다. 중복 웹훅(answered 재배달·finish 재배달)은 이중 집계되지 않는다. 한계를 응답이 스스로 밝힌다(`scope="instance"`) — 인스턴스 메모리라 다중 인스턴스 합산·영속 집계·실 CTI 연동은 **[승인 필요]**(교체점은 `SINK` 하나). 조회는 읽기 전용이고 게이트를 켜지 않는다. 테스트 `tests/test_call_metrics.py` **46건** + `tests/test_console_call_metrics.py` **22건** + `test_ops_stats` 1건 → 전체 **964건 통과**. 변이 검증으로 회귀의 유효성 확인(빈 표본 0% 화, 재배달 이중집계, 전환 표식 누락, 데모 폴백, 자동갱신 미연결 = 전부 실패로 잡힘). 로컬 E2E(실제 소켓·외부망 미접속): 빈 표본 200·`auto_rate=null` → 통화 1건 반영 → 개인정보 미노출 → 잘못된 period 400 → 외부 오리진 403. node 렌더 검증으로 빈/부분/오류/이스케이프 상태까지 확인.
- [ ] CPaaS 활성화 전 체크리스트 이행 — CPAAS_ACTIVATION.md 참조 **[실발신은 승인]**

## 파트너 채널 (제이투모로우원 — 운영 대행 + 수익 배분)

계약·서비스 주체는 고원, 파트너는 영업·운영을 담당하고 수익을 배분한다.
**향후 리셀러(파트너 명의 계약)로 전환될 수 있으므로, 지금은 2계층으로 확장 가능한 형태로만 열어둔다.**

- [x] **파트너(채널) 개념 도입** — `api/partners.py`(의존성 0). 고객사 귀속 레코드의 `partner_id` 는 **nullable** 이고 없으면 직접 계약(`attribution="direct"`)이다 — 파트너가 붙지 않은 고객사를 억지로 어딘가에 소속시키지 않는다. 파트너는 `id`(소문자·숫자·`_-`, **대소문자 정규화**로 `CH-Alpha`/`ch-alpha` 가 별개 파트너로 갈라져 귀속이 쪼개지는 일을 막는다)·이름·상태(`active`/`suspended`)만 갖는다. 화면 노출은 아직 없고 API 만 연다(백로그 지시대로 '스키마만 준비'). **대장은 비어서 시작한다** — 데모 파트너·도입사례를 심지 않는다(회귀로 강제, §8·QUALITY_BAR 3). `GET /api/partners`(요약)·`?op=list|accounts|attribution|policy|history`, `POST {op:create|suspend|resume|attach|reassign|detach}`. 한도(파트너 200·고객사 2000·고객사별 변경 50)와 인스턴스 메모리 휘발성을 응답이 스스로 밝힌다(`persistence`) — 영속 저장소 배선은 **[승인 필요]**.
- [x] **매출 귀속 근거** — **귀속을 덮어쓰지 않고 기간으로 쌓는다.** 담당 파트너가 바뀌면 이전 귀속을 지우는 게 아니라 그 시점에 닫고 새 기간을 연다(`periods[]`: partner_id·channel·담당자·from/to·사유·기록자). 정산 분쟁의 9할은 "그때 누구 담당이었냐"이고 마지막 값만 남기는 스키마는 그 질문에 답하지 못한다 — `attribution_at(tenant, at)` 이 임의 시점의 담당을 되짚는다. 기간은 **겹치지도 비지도 않는다**(`check_invariants()` 가 검사하고 요약 응답의 `integrity` 로 드러낸다; 변이 검증으로 실제로 깨진 장부를 잡는 것 확인). 해지도 **지우지 않고 닫는다** — 지난 정산 근거가 사라지면 분쟁에서 진다. 유입 경로 6종(직접·파트너 소개·파트너 운영대행·문의·행사·기존 확장)과 파트너 유무의 **정합성을 강제**한다: `direct` 인데 파트너가 붙거나 `partner_*` 인데 파트너가 없으면 400(모순된 장부를 애초에 만들지 않는다). 계약일은 미래 날짜를 거부한다. **개인정보 최소 수집**: 담당자는 이름만 마스킹 저장(`홍길동`→`홍*동`, 단방향)하고 전화·이메일이 섞이면 저장 전에 400 으로 거부한다 — 장부의 목적은 귀속 증명이지 사람 명부가 아니다. 응답·이력 어디에도 원문 이름이 없다(회귀). **수수료·청구액은 이 모듈이 계산하지 않는다**(근거만 남긴다 — 요율·정산은 계약 확정 후 아래 항목).
- [x] **파트너 역할 권한** — `authorize(role, actor_partner_id, tenant_id, at)` 순수 함수로 판정을 구현·검증했다. `partner_admin` 은 **그 시점에 자기 파트너로 귀속된** 고객사만 본다 — 담당이 넘어간 뒤에는 보이지 않고, 넘어가기 전 기간은 여전히 보인다(시점 인자가 있는 이유). **직접 계약 고객사는 어떤 파트너에게도 보이지 않는다.** 미지 역할·파트너 ID 없는 partner_admin·해지된 고객사는 전부 거부(기본값 deny). 판정은 부작용이 없다(조회가 장부를 바꾸지 않음, 회귀). **활성화는 승인**: 실제 접근 통제 배선 지점 `enforce()` 는 `PARTNER_RBAC_LIVE=1` 전까지 **호출 자체가 RuntimeError「[승인 필요]」로 실패**한다 — 판정이 조용히 '전부 허용'으로 동작해 반쯤 배선된 채 배포되는 일을 막기 위해서다(게이트는 `"1"` 정확 일치, 호출 시점 읽기). `ROLES["partner_admin"]["activated"]=False` 와 요약 응답의 `rbac.note` 가 상태를 드러낸다. 조회·등록 어느 경로도 게이트를 켜지 않는다(회귀).
- [x] **정산 리포트** — `api/settlement.py`(의존성 0, **읽기 전용**) + `partners.attribution_periods()`. 월 단위로 계약(귀속 기간)·이용 실적·수수료 산출 근거를 한 표에 놓고 CSV 로 내보낸다. **요율은 설정값이다** — `PARTNER_RATE_CARD` 환경변수(또는 `set_rate_card()`)로 주입하고 **기본은 미설정**이다. 요율이 없으면 금액을 0 원으로 만들지 않고 `commission_krw=null`+`status="rate_missing"` 로 드러낸다(0 원도 주장이고, 틀린 주장이 청구서로 가면 분쟁이 된다). 잘못된 요율표는 조용히 고치지 않고 `problems[]` 로 내보이며 그 상태로는 쓰지 않는다(범위 밖 %·소수 건당요율·키 형식·통화·vat·version 누락). **요율을 HTTP 로 바꿀 수 없다** — POST/PUT/DELETE 는 405(`Allow: GET, OPTIONS`). **귀속 기간으로 쪼갠다**: 정산월 중간에 담당이 바뀌면 그 날 실적을 점유 시간 비율로 나누되 **최대잉여법**을 써서 쪼갠 건수·금액의 합이 원본과 정확히 같다(반올림으로 통화가 사라지거나 생기지 않는다). 장부가 덮지 못하는 구간(등록 전·해지 후·미등록 고객사)은 버리지 않고 `unattributed` 줄로 남겨 `attention[]` 에 띄운다 — 조용히 사라진 실적이 나중에 분쟁이 된다. **월·일 경계는 KST(UTC+9)** — UTC 로 끊으면 말일 밤 9시간치가 옆 달로 샌다. 금액은 `Decimal` 원 단위 반올림(ROUND_HALF_UP)이고 부가세는 계산하지 않고 요율표 표기를 옮긴다(세무는 사람이 확정). **실적을 지어내지 않는다** — 집계는 `record_usage()` 로 주입된 관측치뿐이고, 없으면 리포트는 빈 표에 합계 `null` 이다(§8 준수, 데모 수치 추가 없음). 산출물은 항상 `status="draft"` — 확정·청구·지급은 **[승인 필요]**. CSV 는 엑셀용 UTF-8 BOM·CRLF 에 **수식 주입 방어**(`=`·`+`·`-`·`@` 로 시작하는 셀 무력화 — 정산 파일은 회계 담당자가 연다)를 두고, 값이 없는 칸은 0 이 아니라 빈 칸이다. 개인정보 없음: 집계는 고객사 단위이고 담당자는 장부의 마스킹 이름만 쓴다(원문 이름·번호 미노출 회귀). 테스트 `tests/test_settlement.py` **74건** → 전체 **1129건 통과**, `settlement` 커버리지 **97%**. 변이 검증으로 회귀의 유효성 확인(CSV 수식가드 제거·요율없음을 0원으로·단순 반올림 배분·UTC 경계·미귀속 조각 버림·매출 없이 금액 생성 = 전부 실패로 잡힘). 로컬 E2E(실제 소켓·외부망 미접속): 요약 200 → 요율 미설정 리포트 200(금액 null) → 요율 주입 후 200(근거 문자열 확인) → CSV 200(BOM·attachment) → 잘못된 월 400 → 미래 월 400 → POST 405 → 외부 오리진 403. 저장은 인스턴스 메모리(휘발) — 영속 집계·청구 시스템 연동은 **[승인 필요]**.
- [ ] **2계층 확장 여지 확보** — 테넌트 조회 경로에 파트너 필터가 나중에 끼어들 수 있도록 쿼리 계층 정리. 지금 화이트라벨은 구현하지 않음
      · *착수분*: `partners.visible_tenants(role, partner_id, at)` 를 조회 경로가 쓸 **단일 진입점**으로 두었다(리셀러 필터는 여기서만 끼어들면 된다). 남은 일은 기존 테넌트 조회 경로들을 이 함수로 모으는 것.

> 원칙: 파트너 관련 기능도 **코드는 만들되 활성화는 승인**. 실제 정산·청구는 계약서 확정 후.

