# CPaaS 실연계 활성화 런북 (P1 스파이크 — 실통화 1건)

설계서 §12.2 "P1이 전부다. 실통화 1건이 되면 영업의 질이 완전히 달라진다."
현재 상태: **코드 준비 완료 · 실회선 OFF**. 데모 연동·테스트는 이미 1회 성공(크레딧 소진으로 대기 중).

---

## 0. 지금 막혀 있는 것
| 항목 | 상태 |
|---|---|
| 어댑터·웹훅 코드 | ✅ 완료 (`api/sip_adapter.py`, `api/_guard.py`, `api/health.py`) |
| sim 모드 동작 | ✅ 정상 |
| **CPaaS 크레딧** | ⛔ 소진 → 충전 필요 |
| **발신번호 사전등록** | ⚠️ 확인 필요 (§10.2 법적 의무) |

---

## 1. 활성화 전 필수 체크 (법·계약)

- [ ] **CPaaS 크레딧 충전** — 통화·SMS 발신 잔액
- [ ] **발신번호 사전등록** (§10.2 「전기통신사업법」 제84조)
      - 아웃바운드 콜 **및 Visual IVR 링크 SMS** 모두 사전등록 번호로만 발송
      - 타사 명의 번호 대행 발신 시 **명의 확인 서류** 보관
- [ ] **AI 고지 멘트** 첫 안내에 포함 (§10.1) — "AI 상담원이 응대합니다"
- [ ] **개인정보** — 통화 녹음·전사 보관 시 처리방침 반영, 국외 엔진 사용 시 **국외이전 고지**(§10.3)
- [ ] 테스트 대상 번호는 **본인/사내 번호**로 한정 (실고객 발신 금지)

---

## 2. 환경변수 (Vercel · 서버 전용)

| 변수 | 값 | 비고 |
|---|---|---|
| `CPAAS_LIVE` | `1` | **마지막에** 켠다. 이 값이 0이면 전부 dry-run |
| `CPAAS_PROVIDER` | `twilio` 등 | 사용 CPaaS |
| `CPAAS_WEBHOOK_TOKEN` | 긴 랜덤 | 웹훅 인증(`?t=` 또는 `X-Webhook-Token`) |
| 제공사 키 | 계정 SID/토큰 등 | **코드 하드코딩 금지** |

> 순서 주의: 키·웹훅 토큰 → 웹훅 URL 등록 → 헬스체크 → **마지막에 `CPAAS_LIVE=1`**

---

## 3. 활성화 절차

1. 크레딧 충전 · 발신번호 등록 완료 확인
2. Vercel 환경변수 입력 (위 표, `CPAAS_LIVE`는 아직 `0`)
3. CPaaS 콘솔에 **웹훅 URL** 등록: `https://<도메인>/api/voice?t=<CPAAS_WEBHOOK_TOKEN>`
4. `GET /api/health` 로 플래그 상태 확인 (`cpaas_webhook: true`)
5. **sim으로 최종 리허설** — 인바운드 시나리오 1건 정상 통과 확인
6. `CPAAS_LIVE=1` 설정 → 재배포
7. **본인 번호로 인바운드 1콜** — ring → answered → STT → 응답 → hangup
8. 확인: 바지인 동작(§5.1), 인식 실패 2회 시 화면 전환·3회 시 상담사 이관(폴백)
9. 로그·이벤트 기록 확인 (latency, pii_masked)

---

## 4. 롤백 (문제 발생 시)

`CPAAS_LIVE=0` → 재배포. 즉시 sim으로 돌아가며 실발신이 중단된다.

---

## 5. P1 완료 판정 (§12.3)

- [ ] 실회선 인입 통화 처리
- [ ] 바지인(TTS 중 고객 발화 시 즉시 중단)
- [ ] AI 실패 시 상담사/기존 IVR 자동 폴백
- [ ] 통화 이벤트·전사 기록, PII 마스킹 적용

> 자동화는 이 문서의 **1~2단계(코드·설정 준비)까지만** 수행한다. `CPAAS_LIVE=1` 전환과 실발신은 **사람이 직접** 한다.
